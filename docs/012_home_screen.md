# ホーム画面の最終仕上げ

## 概要
ホーム画面にストリーク・感情タグ・話題クイック選択ボタンを統合し、毎日開きたくなるリテンション画面を完成させる。

## 背景
REQUIREMENTS.md §5-2（Screen 3）でストリークカードが「ホームの主役要素」と定義されている。現在の実装は概ね完成しているが、感情タグ表示（#005）とストリーク計算（#006）の完成後に統合確認が必要。

## TODO
- [x] `HomeViewModel.load()` が `StorageService` からセッション・タグを正しく読み込む（確認済み）
- [x] 今日セッション完了済みの場合、CTAボタンのテキストを「もう一度話す」に変える（`sessionCompletedToday` プロパティ追加）
- [x] `vm.topEmotions` が空のとき感情セクションを非表示にする（topicSection は常に表示、感情セクションは不要）
- [x] 話題クイック選択ボタン（仕事 / 人間関係 / 将来 / なんとなく）を2×2グリッドで表示し、タップでそのトピックのチャットを開始する（実装済み）
- [x] Free ユーザーで残り0回の場合、CTAボタンに「残り0回 / Proにアップグレード」表示を追加し、インディゴグラデーションに変更
- [x] アプリがフォアグラウンドに戻った時（`ScenePhase.active`）に `vm.load()` を再実行して表示を更新する
- [ ] ストリーク「7日・30日」の達成時に特別なアニメーション（Lottie）を表示する（任意・v1.1以降）
- [x] `NavigationStack` を維持（v1.0はタブバーなし・単一スタック）

## 完了条件
- ストリーク数が正確に表示される
- 今週の感情タグが最大3つ表示される
- 話題クイック選択からそのトピックのチャットが開始できる

## 依存関係
- 前提: #005（感情タグ）、#006（ストリーク計算）
- ブロック: なし

## 関連ファイル
- `honne/Views/Home/HomeView.swift`
- `honne/ViewModels/HomeViewModel.swift`
- `honne/Services/StorageService.swift`
